/**
 * Placeholder — built out in Phase 7 (Frontend Engineering Plan).
 * Exists now only so the route resolves inside the Phase 2 layout shell.
 */
export default function TasksPage() {
  return <p>Tasks — built in Phase 7.</p>;
}
