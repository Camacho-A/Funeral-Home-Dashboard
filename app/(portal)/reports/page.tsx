/**
 * Placeholder — built out in Phase 8 (Frontend Engineering Plan).
 * Exists now only so the route resolves inside the Phase 2 layout shell.
 */
export default function ReportsPage() {
  return <p>Reports — built in Phase 8.</p>;
}
