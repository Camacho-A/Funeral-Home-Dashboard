/**
 * Placeholder — built out in Phase 5 (Frontend Engineering Plan).
 * Exists now only so the route resolves inside the Phase 2 layout shell.
 */
export default function DashboardPage() {
  return <p>Dashboard — built in Phase 5.</p>;
}
